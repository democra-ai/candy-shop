{
  "name": "candy-shop-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Candy Shop backend on Cloudflare Workers (Hono + D1)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply candy-shop-db --remote",
    "d1:migrate:local": "wrangler d1 migrations apply candy-shop-db --local",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "stripe": "^18.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "~5.9.3",
    "wrangler": "^4.82.2"
  }
}
